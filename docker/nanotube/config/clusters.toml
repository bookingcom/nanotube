[[cluster]]
name = "carbon0"
type = "jump" # cluster type defines routing inside a cluster
    [[cluster.hosts]]
    name = "go-carbon-0" # FQDN or IP of the host
    port = 2003 # optional, overrides setting in main config
    index = 0 # index in the hash ring

[[cluster]]
name = "carbon1"
type = "jump" # cluster type defines routing inside a cluster
    [[cluster.hosts]]
    name = "go-carbon-1" # FQDN or IP of the host
    port = 2003 # optional, overrides setting in main config
    index = 0 # index in the hash ring
