image: docker.artifactory.booking.com/projects/golang/golang-ci:1.13

stages:
  - package

package:
  stage: package
  tags: [ docker ]
  only:
    - tags
  except:
    - branches
    - triggers
  script:
    - ./.build/gitlab-package.sh
