image: docker.artifactory.booking.com/projects/golang/golang-ci:1.14

stages:
  - package

package_subnanotube:
  stage: package
  tags: 
    - docker
  only:
    - tags
    # tag should look like this
    - /^nanotube-\d+-\d+$/
  # except:
  #   - branches
  script:
    - ./.build/package-subnt.sh
